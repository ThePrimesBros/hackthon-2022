{% extends 'base.html.twig' %}

{% block title %}Page{% endblock %}

{% block body %}
    <h1>Page</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ page.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ page.title }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ page.text }}</td>
            </tr>
            <tr>
                <th>Seo_title</th>
                <td>{{ page.seoTitle }}</td>
            </tr>
            <tr>
                <th>Seo_description</th>
                <td>{{ page.seoDescription }}</td>
            </tr>
            <tr>
                <th>Html</th>
                <td>{{ page.html }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ page.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_page_index') }}">back to list</a>

    <a href="{{ path('app_page_edit', {'id': page.id}) }}">edit</a>

    {{ include('page/_delete_form.html.twig') }}
{% endblock %}
