{% extends 'base.html.twig' %}

{% block title %}Protocol{% endblock %}

{% block body %}
    <h1>Protocol</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ protocol.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ protocol.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ protocol.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_protocol_index') }}">back to list</a>

    <a href="{{ path('app_protocol_edit', {'id': protocol.id}) }}">edit</a>

    {{ include('protocol/_delete_form.html.twig') }}
{% endblock %}
