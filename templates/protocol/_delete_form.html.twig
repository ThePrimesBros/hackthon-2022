<form method="post" action="{{ path('app_protocol_delete', {'id': protocol.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ protocol.id) }}">
    <button class="btn">Delete</button>
</form>
